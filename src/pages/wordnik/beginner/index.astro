---
import Layout from '../../../layouts/Layout.astro'
---


<Layout title="Beginner Words">

   <div class="container mx-auto px-4 bg-white min-h-screen"></div> 


</Layout>



<script is:inline>

const container=document.querySelector(".container")

const renderwords= async ()=>{
    let box='';
        const res=await fetch("https://server-five-dusky.vercel.app/games")
        const data=await res.json()     
        data.forEach((el)=>{
          box+=`
          <div class="rounded-lg shadow-lg my-2 p-3 bg-white text-gray-900 dark:bg-slate-700 dark:text-white">
            <h3 class="text-xl font-bold">${el.title}</h3>
            <p class="text-lg">${el.desc}</p>
           <a href="details?id=${el.id}"><button class="bg-blue-600 text-white rounded-lg py-2 px-3">Start</button></a>
            </div>
            `

        })
        container.innerHTML=box
    }



window.addEventListener("DOMContentLoaded", ()=> renderwords())








</script>
