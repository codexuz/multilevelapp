---
export interface Props {
	qname: string;
	qvalue: string;
  choice:string
}

const { qname, qvalue, choice } = Astro.props;
---

<div class="question">
<input type="radio" name={qname} value={qvalue}> {choice} <br>
</div>
